# Server port
PORT=3210

# Node environment (development or production)
# In production mode:
# - CORS is restricted to ALLOWED_ORIGINS
# - Error messages are sanitized (no stack traces)
# - Security headers (CSP, HSTS) are enabled
NODE_ENV=development

# Allowed origins for CORS (comma-separated, only used in production)
# If not set in production, only same-origin requests are allowed
# Example: ALLOWED_ORIGINS=https://example.com,https://www.example.com
ALLOWED_ORIGINS=

# Disable ImageMagick (use Sharp for all image processing)
# Set to 'true' to disable ImageMagick usage
DISABLE_IMAGEMAGICK=false

# ===========================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ===========================================
# 1. Set NODE_ENV=production
# 2. Configure ALLOWED_ORIGINS if you need cross-origin requests
# 3. Use HTTPS (required for HSTS and secure cookies)
# 4. Consider placing behind a reverse proxy (nginx) for:
#    - SSL termination
#    - Additional rate limiting
#    - Request filtering
# 5. Ensure FFmpeg is installed and in PATH
# 6. Set up log rotation for production logs
# 7. Configure proper firewall rules
# 8. Run as a non-root user
